meta:
  platform: jira
  document_type: task
  version: 1.0.0
  last_updated: 2025-11-11
  status: stable

template:
  title: "{{ context.componentName | default(figma.component_name) | default('Component Implementation') }}"
  issue_type: "{{ context.issueType | default('Task') }}"
  priority: "{{ context.priority | default('Medium') }}"
  labels: ["figma", "ui", "component"]
  
  content: |
    ## 🎯 Component Overview
    
    Implement the **{{ context.componentName | default(figma.component_name) | default('Component') }}** component from the design specifications.
    
    **📋 Quick Details:**
    - **Project**: {{ context.projectName | default(project.name) | default('Project') }}
    - **Component**: {{ context.componentName | default(figma.component_name) | default('Component') }}
    - **Issue Type**: {{ context.issueType | default('Component Implementation') }}
    - **Priority**: {{ context.priority | default('Medium') }}
    - **Story Points**: {{ context.storyPoints | default('3') }}
    
    ## 🎨 Design References
    
    **🔗 Figma Design**: [View Component]({{ figma.live_link | default('https://figma.com') }})
    
    {% if figma.screenshot_markdown %}
    **📸 Screenshot**: {{ figma.screenshot_markdown.jira }}
    {% endif %}
    
    ## ✅ Acceptance Criteria
    
    - [ ] **Visual Accuracy**: Component matches design specifications exactly
    - [ ] **Responsive Design**: Works across all supported breakpoints (mobile, tablet, desktop)
    - [ ] **Accessibility**: Meets WCAG 2.1 AA standards with proper ARIA labels
    - [ ] **Testing**: Unit tests implemented and passing
    - [ ] **Performance**: Component renders efficiently
    - [ ] **Documentation**: Component documented with usage examples
    
    ## 🔧 Technical Requirements
    
    **Implementation Tasks:**
    - [ ] **Setup**: Create component file structure
    - [ ] **Styling**: Implement CSS with design tokens
    - [ ] **Logic**: Add component behavior and functionality
    - [ ] **Testing**: Write comprehensive unit tests
    - [ ] **Review**: Code review and quality assurance
    
    ## 🔗 Resources
    
    - **Figma Design**: [{{ figma.live_link | default('Design Link') }}]({{ figma.live_link | default('#') }})
    - **Project**: {{ context.projectName | default(project.name) | default('Project Name') }}
    
    ---
    
    **Generated**: {{ 'now' | date('%Y-%m-%d %H:%M') }} via Figma AI Ticket Generator